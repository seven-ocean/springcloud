spring:
  cloud:
    config:
      name: eurekaclient
      label: master
      discovery:
        enabled: true
        serviceId: eurekaconfig
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/